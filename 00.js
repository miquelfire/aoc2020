/**
 * @param {string} d 
 */
const part1 = async d => {
	const data = d.split('\n').map(e => parseInt(e));
	return null;
}

/**
 * @param {string} d 
 */
const part2 = async d => {
	const data = d.split('\n').map(e => parseInt(e));
	return null;
}

module.exports = {
	part1,
	//part2
};
